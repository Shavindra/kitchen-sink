import React, { useState } from 'react';
import { LayoutFullWidth } from '../../layouts';

export const PageHome: React.FC = (props) => {
    return (
        <LayoutFullWidth>
            <h1>
                Home
            </h1>
        </LayoutFullWidth>
    )
}
